# macOS
.DS_Store

# Dependencies (restore with: npm install)
node_modules/

# Local artifacts — never ship
temporary screenshots/
_originals_backup/
*.log

# Dev-only files — kept locally, never published (GitHub Pages serves the
# whole repo root, so anything tracked here is public on uniquetouch.beauty)
CLAUDE.md
SKILL.md
package.json
package-lock.json
serve.mjs
screenshot.mjs
scroll-sim.mjs
nextjs-transitions/
wireframe_assets/
